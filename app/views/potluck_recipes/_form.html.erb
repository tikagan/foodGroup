<%= form_with(model: potluck_recipe, local: true) do |form| %>
  <% if potluck_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(potluck_recipe.errors.count, "error") %> prohibited this potluck_recipe from being saved:</h2>

      <ul>
      <% potluck_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :potluck_recipe_references %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :potluck_recipe_references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
