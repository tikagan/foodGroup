<p id="notice"><%= notice %></p>

<h1>Grocery Ingredients</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grocery_ingredients.each do |grocery_ingredient| %>
      <tr>
        <td><%= link_to 'Show', grocery_ingredient %></td>
        <td><%= link_to 'Edit', edit_grocery_ingredient_path(grocery_ingredient) %></td>
        <td><%= link_to 'Destroy', grocery_ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grocery Ingredient', new_grocery_ingredient_path %>
